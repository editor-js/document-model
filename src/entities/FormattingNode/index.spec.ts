import { describe } from '@jest/globals';

describe('FormattingNode', () => {
  describe('.length', () => {});

  describe('.getText()', () => {});

  describe('.insertText()', () => {});

  describe('.format()', () => {});
});
