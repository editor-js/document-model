// TODO: temporarily re-exported from model-types until keypath is extracted
// into its own dependency-free utils package (see model-types/src/index.ts).
export { get, set, has, insert, remove, renumberKeys } from '@editorjs/model-types';
